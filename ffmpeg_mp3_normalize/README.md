# Normalize an input file

Script to normalize an input file to Mp3 variable bitrate 64k.

## Setup
On Debian/Ubuntu

     sudo apt-get install ffmpeg python-pip
     pip install ffmpeg-normalize

## Usage

    ./encode_normalize.sh input.mp3
